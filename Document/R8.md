其实。任何史山和力竭的代码引擎都有一个共同点：硬编码的死凑
就比如臭名昭著的LOD纯是一堆硬编码的结果
逻辑和代码一定要是有生命的，它自己应当知道怎么渲染自己。它应当知道自己在屏幕上只占一个像素的时候，就直接”思考“类似：”这小格子的模型看起来绿的最多，涂绿得了“，不需要我们定义什么多少距离干什么事。它也应当知道一个大墙后面即使有着亿万模型，也能”想“到：”这墙全挡住了，渲染一下墙砖就收工“

不过咱们现在需要专精的问题就是遮挡关系：怎么样才能完美避开硬编码的队列数枷锁